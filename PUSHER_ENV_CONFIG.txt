# Add these lines to your .env file to fix the chat functionality

# Broadcasting Configuration
BROADCAST_DRIVER=pusher

# Pusher Configuration (Backend)
PUSHER_APP_ID=2047345
PUSHER_APP_KEY=5c02e54d01ca577ae77e
PUSHER_APP_SECRET=3ad793a15a653af09cd6
PUSHER_APP_CLUSTER=ap1

# Pusher TLS Settings (IMPORTANT: Set to false since Force TLS is OFF in Pusher dashboard)
PUSHER_USE_TLS=false
PUSHER_ENCRYPTED=false

# Pusher Frontend Configuration (for Vite)
VITE_PUSHER_APP_KEY=5c02e54d01ca577ae77e
VITE_PUSHER_APP_CLUSTER=ap1
VITE_PUSHER_FORCE_TLS=false

# Optional: Pusher Host and Port (usually not needed)
# PUSHER_HOST=api.pusherapp.com
# PUSHER_PORT=443
# PUSHER_SCHEME=https
